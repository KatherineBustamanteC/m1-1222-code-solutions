/* exported getFirstChar */
function getFirstChar(string) { // define a function with 1 parameter
  var array = []; // create a variable
  for (var i = 0; i < string.length; i++) { // create a for loop to get the first character
    array = string[0]; // get the first character of the string at zero index
  } return array; // return the value
}
